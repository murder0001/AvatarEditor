local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = " :3 ",
   LoadingTitle = ":3",
   LoadingSubtitle = "By Rico",
   ConfigurationSaving = {
      Enabled = false,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = ""
   },
   Discord = {
      Enabled = false,
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },
   KeySystem = false, -- Set this to true to use our key system
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided",
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"Hello"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local HomeTab = Window:CreateTab("Home", nil) -- Title, Image
local Section = HomeTab:CreateSection("Home")

local Button = HomeTab:CreateButton({
   Name = "Headless Permanent",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function setHeadTransparency()
    while true do
        wait() -- Wait a short amount of time before each execution
        if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Head") then
            local head = game.Players.LocalPlayer.Character.Head

            head.Transparency = 1

            for i, v in pairs(head:GetChildren()) do
                if v:IsA("Decal") then
                    v.Transparency = 1
                end
            end
        end
    end
end

setHeadTransparency()

   end,
})

local Button = HomeTab:CreateButton({
   Name = "Korblox Permanent",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function setCharacterParts()
    while true do
        wait() -- Wait a short amount of time before each execution
        local ply = game.Players.LocalPlayer

        if ply.Character then
            local chr = ply.Character

            chr.RightLowerLeg.MeshId = "902942093"
            chr.RightLowerLeg.Transparency = 1
            chr.RightUpperLeg.MeshId = "http://www.roblox.com/asset/?id=902942096"
            chr.RightUpperLeg.TextureID = "http://roblox.com/asset/?id=902843398"
            chr.RightFoot.MeshId = "902942089"
            chr.RightFoot.Transparency = 1
        end
    end
end

setCharacterParts()

   end,
})

local HatsTab = Window:CreateTab("Hats", nil) -- Title, Image
local Section = HatsTab:CreateSection("Hats")
local BackTab = Window:CreateTab("Back", nil) -- Title, Image
local Section = BackTab:CreateSection("Back")
local HairsTab = Window:CreateTab("Hairs", nil) -- Title, Image
local Section = HairsTab:CreateSection("Hairs")

local Button = HatsTab:CreateButton({
   Name = "Clockwork's Headphones",
   Callback = function()
   -- The function that takes place when the button is pressed
	

   end,
})

local Button = HatsTab:CreateButton({
   Name = "The Void Star",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 1125510
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "Silver King of the Night",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 439945661
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "Frozen Horns of the Frigid Planes",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 74891470
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "Fiery Horns of the Netherworld",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 215718515
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "The Classic ROBLOX Fedora",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 1029025
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "Valkyrie Helm",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 1365767
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "Green Queen of the Night",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 553970961
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "Black Iron King of the Night",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 439946249
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local FacesTab = Window:CreateTab("Faces", nil) -- Title, Image
local Section = FacesTab:CreateSection("Faces")

local Button = FacesTab:CreateButton({
   Name = "Red Glowing Eyes + RESET WILL KEEP",
   Callback = function()
   -- The function that takes place when the button is pressed
   local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function setFaceTexture(character)
    local head = character:WaitForChild("Head")
    local face = head:FindFirstChild("face") or head:FindFirstChild("Face")
    if face then
        face.Texture = "rbxassetid://179693472"
    end
end

LocalPlayer.CharacterAdded:Connect(setFaceTexture) -- Call the function when the character is added

-- Check if the character is already loaded
if LocalPlayer.Character then
    setFaceTexture(LocalPlayer.Character)
end

   end,
})

local Button = FacesTab:CreateButton({
   Name = "Yum! + RESET WILL KEEP",
   Callback = function()
   -- The function that takes place when the button is pressed
   local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function setFaceTexture(character)
    local head = character:WaitForChild("Head")
    local face = head:FindFirstChild("face") or head:FindFirstChild("Face")
    if face then
        face.Texture = "rbxassetid://26018945"
    end
end

LocalPlayer.CharacterAdded:Connect(setFaceTexture) -- Call the function when the character is added

-- Check if the character is already loaded
if LocalPlayer.Character then
    setFaceTexture(LocalPlayer.Character)
end
   end,
})

local Button = FacesTab:CreateButton({
   Name = "Super Super Happy Face + RESET WILL KEEP",
   Callback = function()
   -- The function that takes place when the button is pressed
   local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function setFaceTexture(character)
    local head = character:WaitForChild("Head")
    local face = head:FindFirstChild("face") or head:FindFirstChild("Face")
    if face then
        face.Texture = "rbxassetid://494290547"
    end
end
   end,
})

local Button = FacesTab:CreateButton({
   Name = "The Dog Whisperer + RESET WILL KEEP",
   Callback = function()
   -- The function that takes place when the button is pressed
   local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function setFaceTexture(character)
    local head = character:WaitForChild("Head")
    local face = head:FindFirstChild("face") or head:FindFirstChild("Face")
    if face then
        face.Texture = "rbxassetid://34764373"
    end
end

LocalPlayer.CharacterAdded:Connect(setFaceTexture) -- Call the function when the character is added

-- Check if the character is already loaded
if LocalPlayer.Character then
    setFaceTexture(LocalPlayer.Character)
end
   end,
})

local Button = FacesTab:CreateButton({
   Name = "Playful Vampire + RESET WILL KEEP",
   Callback = function()
   -- The function that takes place when the button is pressed
   local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function setFaceTexture(character)
    local head = character:WaitForChild("Head")
    local face = head:FindFirstChild("face") or head:FindFirstChild("Face")
    if face then
        face.Texture = "rbxassetid://2409281591"
    end
end

LocalPlayer.CharacterAdded:Connect(setFaceTexture) -- Call the function when the character is added

-- Check if the character is already loaded
if LocalPlayer.Character then
    setFaceTexture(LocalPlayer.Character)
end
   end,
})

local Button = FacesTab:CreateButton({
   Name = "ROBLOX Madness Face + RESET WILL KEEP",
   Callback = function()
   -- The function that takes place when the button is pressed
   local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function setFaceTexture(character)
    local head = character:WaitForChild("Head")
    local face = head:FindFirstChild("face") or head:FindFirstChild("Face")
    if face then
        face.Texture = "rbxassetid://129900258"
    end
end

LocalPlayer.CharacterAdded:Connect(setFaceTexture) -- Call the function when the character is added

-- Check if the character is already loaded
if LocalPlayer.Character then
    setFaceTexture(LocalPlayer.Character)
end
   end,
})

local Button = FacesTab:CreateButton({
   Name = "Catching Snowflakes + RESET WILL KEEP",
   Callback = function()
   -- The function that takes place when the button is pressed
   local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function setFaceTexture(character)
    local head = character:WaitForChild("Head")
    local face = head:FindFirstChild("face") or head:FindFirstChild("Face")
    if face then
        face.Texture = "rbxassetid://1213444061"
    end
end

LocalPlayer.CharacterAdded:Connect(setFaceTexture) -- Call the function when the character is added

-- Check if the character is already loaded
if LocalPlayer.Character then
    setFaceTexture(LocalPlayer.Character)
end
   end,
})


local Button = HatsTab:CreateButton({
   Name = "Blackvalk",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 124730194
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "Purple Sparkle Time Fedora",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 63043890
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "Black Sparkle Time Fedora",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 259423244
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HatsTab:CreateButton({
   Name = "Dominus Vespertilio",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 96103379
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = BackTab:CreateButton({
   Name = "King's Omen Blade",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 14330807314
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})  

local Button = BackTab:CreateButton({
   Name = "Immortal Sword: Venom's Bite",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 638089422
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})  

local Button = HairsTab:CreateButton({
   Name = "Stickmasterluke's Peanut Butter Sparkle Time",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 169444294
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})  


local Button = HatsTab:CreateButton({
   Name = "Lord of the Federation",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 88885069
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})  

local Button = HatsTab:CreateButton({
   Name = "Gold Emperor of the Night",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 878895806
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})  

local Button = HatsTab:CreateButton({
   Name = "Subarctic Commando",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 39247441
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})  

local Button = BackTab:CreateButton({
   Name = "Amethyst Edge Blade",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 13415565815
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})  

local Button = HomeTab:CreateButton({
   Name = "F1 - F3",
   Callback = function()
   -- The function that takes place when the button is pressed
-- Silver King of the Night
local function onKeyPress(input)
    if input.KeyCode == Enum.KeyCode.F1 then
        -- Check if the chat bar is focused
        if game:GetService("UserInputService"):GetFocusedTextBox() then
            return
        end

        -- Your code here
        local function l(a, b)
            local weld = Instance.new("Weld")
            weld.Part0 = a.Parent
            weld.Part1 = b.Parent
            weld.C0 = a.CFrame
            weld.C1 = b.CFrame
            weld.Parent = a.Parent
            return weld
        end

        local function createWeld(name, parent, part0, part1, c0, c1)
            local weld = Instance.new("Weld")
            weld.Name = name
            weld.Part0 = part0
            weld.Part1 = part1
            weld.C0 = c0
            weld.C1 = c1
            weld.Parent = parent
            return weld
        end

        local function findAttachment(parent, name)
            for _, child in pairs(parent:GetChildren()) do
                if child:IsA("Attachment") and child.Name == name then
                    return child
                elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
                    local found = findAttachment(child, name)
                    if found then
                        return found
                    end
                end
            end
        end

        local function attach(asset, character)
            asset.Parent = character
            local handle = asset:FindFirstChild("Handle")
            if handle then
                local attachment = handle:FindFirstChildOfClass("Attachment")
                if attachment then
                    local foundAttachment = findAttachment(character, attachment.Name)
                    if foundAttachment then
                        l(foundAttachment, attachment)
                    end
                else
                    local head = character:FindFirstChild("Head")
                    if head then
                        local c0 = CFrame.new(0, 0.5, 0)
                        local c1 = handle.AttachmentPoint
                        createWeld("Weld", head, head, handle, c0, c1)
                    end
                end
            end
        end

        local assetId = 439945661
        local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
        attach(asset, game.Players.LocalPlayer.Character)
    end
end

-- Connect the key press event
game:GetService("UserInputService").InputBegan:Connect(onKeyPress)


---------------------------------------

-- Function to inject the code when 'F2' is pressed
local function onKeyPress(input)
    if input.KeyCode == Enum.KeyCode.F2 then
        -- Check if the chat bar is focused
        if game:GetService("UserInputService"):GetFocusedTextBox() then
            return
        end

        -- Your code here
        local function l(a, b)
            local weld = Instance.new("Weld")
            weld.Part0 = a.Parent
            weld.Part1 = b.Parent
            weld.C0 = a.CFrame
            weld.C1 = b.CFrame
            weld.Parent = a.Parent
            return weld
        end

        local function createWeld(name, parent, part0, part1, c0, c1)
            local weld = Instance.new("Weld")
            weld.Name = name
            weld.Part0 = part0
            weld.Part1 = part1
            weld.C0 = c0
            weld.C1 = c1
            weld.Parent = parent
            return weld
        end

        local function findAttachment(parent, name)
            for _, child in pairs(parent:GetChildren()) do
                if child:IsA("Attachment") and child.Name == name then
                    return child
                elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
                    local found = findAttachment(child, name)
                    if found then
                        return found
                    end
                end
            end
        end

        local function attach(asset, character)
            asset.Parent = character
            local handle = asset:FindFirstChild("Handle")
            if handle then
                local attachment = handle:FindFirstChildOfClass("Attachment")
                if attachment then
                    local foundAttachment = findAttachment(character, attachment.Name)
                    if foundAttachment then
                        l(foundAttachment, attachment)
                    end
                else
                    local head = character:FindFirstChild("Head")
                    if head then
                        local c0 = CFrame.new(0, 0.5, 0)
                        local c1 = handle.AttachmentPoint
                        createWeld("Weld", head, head, handle, c0, c1)
                    end
                end
            end
        end

        local assetId = 878895806
        local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
        attach(asset, game.Players.LocalPlayer.Character)
    end
end

-- Connect the key press event
game:GetService("UserInputService").InputBegan:Connect(onKeyPress)

-------------------------------

-- Gold Emperor of the Night
local function onKeyPress(input)
    if input.KeyCode == Enum.KeyCode.F3 then
        -- Check if the chat bar is focused
        if game:GetService("UserInputService"):GetFocusedTextBox() then
            return
        end

        -- Your code here
        local function l(a, b)
            local weld = Instance.new("Weld")
            weld.Part0 = a.Parent
            weld.Part1 = b.Parent
            weld.C0 = a.CFrame
            weld.C1 = b.CFrame
            weld.Parent = a.Parent
            return weld
        end

        local function createWeld(name, parent, part0, part1, c0, c1)
            local weld = Instance.new("Weld")
            weld.Name = name
            weld.Part0 = part0
            weld.Part1 = part1
            weld.C0 = c0
            weld.C1 = c1
            weld.Parent = parent
            return weld
        end

        local function findAttachment(parent, name)
            for _, child in pairs(parent:GetChildren()) do
                if child:IsA("Attachment") and child.Name == name then
                    return child
                elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
                    local found = findAttachment(child, name)
                    if found then
                        return found
                    end
                end
            end
        end

        local function attach(asset, character)
            asset.Parent = character
            local handle = asset:FindFirstChild("Handle")
            if handle then
                local attachment = handle:FindFirstChildOfClass("Attachment")
                if attachment then
                    local foundAttachment = findAttachment(character, attachment.Name)
                    if foundAttachment then
                        l(foundAttachment, attachment)
                    end
                else
                    local head = character:FindFirstChild("Head")
                    if head then
                        local c0 = CFrame.new(0, 0.5, 0)
                        local c1 = handle.AttachmentPoint
                        createWeld("Weld", head, head, handle, c0, c1)
                    end
                end
            end
        end

        local assetId = 1125510
        local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
        attach(asset, game.Players.LocalPlayer.Character)
    end
end

-- Connect the key press event
game:GetService("UserInputService").InputBegan:Connect(onKeyPress)

----------------------------------

-- Blue Queen of the Night
local function onKeyPress(input)
    if input.KeyCode == Enum.KeyCode.F4 then
        -- Check if the chat bar is focused
        if game:GetService("UserInputService"):GetFocusedTextBox() then
            return
        end

        -- Your code here
        local function l(a, b)
            local weld = Instance.new("Weld")
            weld.Part0 = a.Parent
            weld.Part1 = b.Parent
            weld.C0 = a.CFrame
            weld.C1 = b.CFrame
            weld.Parent = a.Parent
            return weld
        end

        local function createWeld(name, parent, part0, part1, c0, c1)
            local weld = Instance.new("Weld")
            weld.Name = name
            weld.Part0 = part0
            weld.Part1 = part1
            weld.C0 = c0
            weld.C1 = c1
            weld.Parent = parent
            return weld
        end

        local function findAttachment(parent, name)
            for _, child in pairs(parent:GetChildren()) do
                if child:IsA("Attachment") and child.Name == name then
                    return child
                elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
                    local found = findAttachment(child, name)
                    if found then
                        return found
                    end
                end
            end
        end

        local function attach(asset, character)
            asset.Parent = character
            local handle = asset:FindFirstChild("Handle")
            if handle then
                local attachment = handle:FindFirstChildOfClass("Attachment")
                if attachment then
                    local foundAttachment = findAttachment(character, attachment.Name)
                    if foundAttachment then
                        l(foundAttachment, attachment)
                    end
                else
                    local head = character:FindFirstChild("Head")
                    if head then
                        local c0 = CFrame.new(0, 0.5, 0)
                        local c1 = handle.AttachmentPoint
                        createWeld("Weld", head, head, handle, c0, c1)
                    end
                end
            end
        end

        local assetId = 553970606
        local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
        attach(asset, game.Players.LocalPlayer.Character)
    end
end

-- Connect the key press event
game:GetService("UserInputService").InputBegan:Connect(onKeyPress)

----------------------------------------

-- Green Queen of the Night
local function onKeyPress(input)
    if input.KeyCode == Enum.KeyCode.F5 then
        -- Check if the chat bar is focused
        if game:GetService("UserInputService"):GetFocusedTextBox() then
            return
        end

        -- Your code here
        local function l(a, b)
            local weld = Instance.new("Weld")
            weld.Part0 = a.Parent
            weld.Part1 = b.Parent
            weld.C0 = a.CFrame
            weld.C1 = b.CFrame
            weld.Parent = a.Parent
            return weld
        end

        local function createWeld(name, parent, part0, part1, c0, c1)
            local weld = Instance.new("Weld")
            weld.Name = name
            weld.Part0 = part0
            weld.Part1 = part1
            weld.C0 = c0
            weld.C1 = c1
            weld.Parent = parent
            return weld
        end

        local function findAttachment(parent, name)
            for _, child in pairs(parent:GetChildren()) do
                if child:IsA("Attachment") and child.Name == name then
                    return child
                elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
                    local found = findAttachment(child, name)
                    if found then
                        return found
                    end
                end
            end
        end

        local function attach(asset, character)
            asset.Parent = character
            local handle = asset:FindFirstChild("Handle")
            if handle then
                local attachment = handle:FindFirstChildOfClass("Attachment")
                if attachment then
                    local foundAttachment = findAttachment(character, attachment.Name)
                    if foundAttachment then
                        l(foundAttachment, attachment)
                    end
                else
                    local head = character:FindFirstChild("Head")
                    if head then
                        local c0 = CFrame.new(0, 0.5, 0)
                        local c1 = handle.AttachmentPoint
                        createWeld("Weld", head, head, handle, c0, c1)
                    end
                end
            end
        end

        local assetId = 553970961
        local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
        attach(asset, game.Players.LocalPlayer.Character)
    end
end

-- Connect the key press event
game:GetService("UserInputService").InputBegan:Connect(onKeyPress)

------------------------

-- Valkyrie Helm
local function onKeyPress(input)
    if input.KeyCode == Enum.KeyCode.F6 then
        -- Check if the chat bar is focused
        if game:GetService("UserInputService"):GetFocusedTextBox() then
            return
        end

        -- Your code here
        local function l(a, b)
            local weld = Instance.new("Weld")
            weld.Part0 = a.Parent
            weld.Part1 = b.Parent
            weld.C0 = a.CFrame
            weld.C1 = b.CFrame
            weld.Parent = a.Parent
            return weld
        end

        local function createWeld(name, parent, part0, part1, c0, c1)
            local weld = Instance.new("Weld")
            weld.Name = name
            weld.Part0 = part0
            weld.Part1 = part1
            weld.C0 = c0
            weld.C1 = c1
            weld.Parent = parent
            return weld
        end

        local function findAttachment(parent, name)
            for _, child in pairs(parent:GetChildren()) do
                if child:IsA("Attachment") and child.Name == name then
                    return child
                elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
                    local found = findAttachment(child, name)
                    if found then
                        return found
                    end
                end
            end
        end

        local function attach(asset, character)
            asset.Parent = character
            local handle = asset:FindFirstChild("Handle")
            if handle then
                local attachment = handle:FindFirstChildOfClass("Attachment")
                if attachment then
                    local foundAttachment = findAttachment(character, attachment.Name)
                    if foundAttachment then
                        l(foundAttachment, attachment)
                    end
                else
                    local head = character:FindFirstChild("Head")
                    if head then
                        local c0 = CFrame.new(0, 0.5, 0)
                        local c1 = handle.AttachmentPoint
                        createWeld("Weld", head, head, handle, c0, c1)
                    end
                end
            end
        end

        local assetId = 1365767
        local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
        attach(asset, game.Players.LocalPlayer.Character)
    end
end

-- Connect the key press event
game:GetService("UserInputService").InputBegan:Connect(onKeyPress)

-- Valkyrie Helm
local function onKeyPress(input)
    if input.KeyCode == Enum.KeyCode.F6 then
        -- Check if the chat bar is focused
        if game:GetService("UserInputService"):GetFocusedTextBox() then
            return
        end

        -- Your code here
        local function l(a, b)
            local weld = Instance.new("Weld")
            weld.Part0 = a.Parent
            weld.Part1 = b.Parent
            weld.C0 = a.CFrame
            weld.C1 = b.CFrame
            weld.Parent = a.Parent
            return weld
        end

        local function createWeld(name, parent, part0, part1, c0, c1)
            local weld = Instance.new("Weld")
            weld.Name = name
            weld.Part0 = part0
            weld.Part1 = part1
            weld.C0 = c0
            weld.C1 = c1
            weld.Parent = parent
            return weld
        end

        local function findAttachment(parent, name)
            for _, child in pairs(parent:GetChildren()) do
                if child:IsA("Attachment") and child.Name == name then
                    return child
                elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
                    local found = findAttachment(child, name)
                    if found then
                        return found
                    end
                end
            end
        end

        local function attach(asset, character)
            asset.Parent = character
            local handle = asset:FindFirstChild("Handle")
            if handle then
                local attachment = handle:FindFirstChildOfClass("Attachment")
                if attachment then
                    local foundAttachment = findAttachment(character, attachment.Name)
                    if foundAttachment then
                        l(foundAttachment, attachment)
                    end
                else
                    local head = character:FindFirstChild("Head")
                    if head then
                        local c0 = CFrame.new(0, 0.5, 0)
                        local c1 = handle.AttachmentPoint
                        createWeld("Weld", head, head, handle, c0, c1)
                    end
                end
            end
        end

        local assetId = 1365767
        local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
        attach(asset, game.Players.LocalPlayer.Character)
    end
end

-- Connect the key press event
game:GetService("UserInputService").InputBegan:Connect(onKeyPress)
   end,
})

local Button = FacesTab:CreateButton({
    Name = "Prankster + RESET WILL KEEP",
    Callback = function()
    -- The function that takes place when the button is pressed
    local Players = game:GetService("Players")
 local LocalPlayer = Players.LocalPlayer
 
 local function setFaceTexture(character)
     local head = character:WaitForChild("Head")
     local face = head:FindFirstChild("face") or head:FindFirstChild("Face")
     if face then
         face.Texture = "rbxassetid://20052028"
     end
 end
 
 LocalPlayer.CharacterAdded:Connect(setFaceTexture) -- Call the function when the character is added
 
 -- Check if the character is already loaded
 if LocalPlayer.Character then
     setFaceTexture(LocalPlayer.Character)
 end
    end,
 })

local Button = HairsTab:CreateButton({
   Name = "2D Black Messy Hair",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 14390224892
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HairsTab:CreateButton({
   Name = "Black Scene Hair",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 14629140475
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HairsTab:CreateButton({
   Name = "Chestnut Style",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 212967757
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = HairsTab:CreateButton({
   Name = "Black Iron Horns",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 628771505
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})

local Button = BackTab:CreateButton({
   Name = "Chain & Ball",
   Callback = function()
   -- The function that takes place when the button is pressed
   local function l(a, b)
    local weld = Instance.new("Weld")
    weld.Part0 = a.Parent
    weld.Part1 = b.Parent
    weld.C0 = a.CFrame
    weld.C1 = b.CFrame
    weld.Parent = a.Parent
    return weld
end

local function createWeld(name, parent, part0, part1, c0, c1)
    local weld = Instance.new("Weld")
    weld.Name = name
    weld.Part0 = part0
    weld.Part1 = part1
    weld.C0 = c0
    weld.C1 = c1
    weld.Parent = parent
    return weld
end

local function findAttachment(parent, name)
    for _, child in pairs(parent:GetChildren()) do
        if child:IsA("Attachment") and child.Name == name then
            return child
        elseif not (child:IsA("Accessory") or child:IsA("Tool")) then
            local found = findAttachment(child, name)
            if found then
                return found
            end
        end
    end
end

local function attach(asset, parent)
    asset.Parent = parent
    local handle = asset:FindFirstChild("Handle")
    if handle then
        local attachment = handle:FindFirstChildOfClass("Attachment")
        if attachment then
            local foundAttachment = findAttachment(parent, attachment.Name)
            if foundAttachment then
                l(foundAttachment, attachment)
            end
        else
            local head = parent:FindFirstChild("Head")
            if head then
                local cframe = CFrame.new(0, 0.5, 0)
                local attachmentPoint = handle.AttachmentPoint
                createWeld("CustomWeld", parent, head, head, handle, cframe, attachmentPoint)
            end
        end
    end
end

local assetId = 5592534698
local asset = game:GetObjects("rbxassetid://" .. tostring(assetId))[1]
attach(asset, game.Players.LocalPlayer.Character)

   end,
})
